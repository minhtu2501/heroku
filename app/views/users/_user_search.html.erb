<%= search_form_for @search do |f| %>
	<div class="field">
		<%= f.label :name_cont, "User Name"%>
		<%= f.text_field :name_cont %>
	</div>
	<div class="actions"><%= f.submit "Search", class: 'btn-search' %></div>
<% end %>